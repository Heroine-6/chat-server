version: "3.9"

services:
  chat:
    build: .
    container_name: chat
    ports:
      - "8081:8080"
    volumes:
      - ./logging/logs:/app/logs
    networks:
      - chat-network

networks:
  chat-network:
    driver: bridge